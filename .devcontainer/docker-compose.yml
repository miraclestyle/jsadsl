version: "3.8"
services:
  test:
    build: .
    stdin_open: true
    tty: true
    container_name: node-container
    volumes:
      - type: volume
        source: jsadsl
        target: /home/developer/jsadsl

volumes:
  jsadsl: