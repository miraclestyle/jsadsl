const {
  defaultCompare,
  less,
  more,
  equal,
} = require('./compare');
const randomInt = require('./random');
const swap = require('./swap');

module.exports = {
  defaultCompare,
  less,
  more,
  equal,
  randomInt,
  swap,
};
